# Agent 配置示例
# 复制为 config.yaml 并根据需要修改
# 注意：API Key 请在 .env 文件中配置，不要写在这里

agent:
  name: "SimpleAgent"
  model: "glm-5"
  max_tokens: 4096
  temperature: 0.7

# 系统提示词
system_prompt: |
  你是一个有帮助的AI助手。请用中文回答用户的问题。