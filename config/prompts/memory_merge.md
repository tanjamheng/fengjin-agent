已有记忆："{old_memory}"
新信息："{new_fact}"

判断规则：
- 如果两者矛盾（如"喜欢A"→"现在不喜欢A了"），用新信息更新，返回合并后的一句话
- 如果是补充关系（如"在做项目X"→"项目X用Python写的"），合并为一条更完整的
- 如果是两个独立的事实（如"喜欢草莓蛋糕"和"喜欢巧克力蛋糕"），返回 "NO_MERGE"
- 如果不相关，返回 "NO_MERGE"